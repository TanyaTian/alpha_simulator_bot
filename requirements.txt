numpy>=1.26.0
pandas>=2.0.0
scikit-learn>=1.3.0
requests==2.31.0
pytz==2023.3
schedule==1.2.0
PyMySQL>=1.0.2  # 显式指定版本，确保支持 caching_sha2_password
cryptography>=3.4  # 添加 cryptography 支持加密认证
flask
DBUtils>=2.0  # 用于数据库连接池（PooledDB）
aiohttp==3.9.5 # 添加 aiohttp 用于异步HTTP请求
matplotlib>=3.0  # 添加 matplotlib 用于数据可视化
tqdm  # 添加 tqdm 进度条库
openai>=1.0.0  # 添加 OpenAI Python SDK
